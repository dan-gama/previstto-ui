export interface BankDomain {
  id: string;
  code: string;
  name: string;
}

export interface AccountTypeDomain {
  id: string;
  code: string;
  name: string;
}

export interface CreditCardBrandDomain {
  id: string;
  code: string;
  name: string;
}
